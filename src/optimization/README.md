# Optimization Source Module

This directory contains tools and scripts for optimizing models *before* they are deployed.

## Structure
- `quantization/`: Scripts for PTQ and QAT.
- `pruning/`: Tools for model pruning.
- `conversion/`: Converters (e.g., PyTorch -> ONNX -> TFLite).
