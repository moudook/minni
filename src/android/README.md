# Android Source Module

This directory contains Android-specific implementation code.

## Structure
- `MainActivity.kt`: Entry point (if applicable).
- `tflite/`: TensorFlow Lite specific wrappers.
- `onnx/`: ONNX Runtime specific wrappers.
- `ui/`: User interface components for the demo app.
- `services/`: Background services (WorkManager/ForegroundService).
