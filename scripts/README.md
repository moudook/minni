# Scripts

This directory contains automation scripts for development and maintenance.

## Structure
- `setup/`: Environment setup scripts.
- `build/`: Build automation (Gradle wrappers, CMake helpers).
- `test/`: Test runners.
- `deploy/`: Deployment scripts (ADB push, model syncing).
